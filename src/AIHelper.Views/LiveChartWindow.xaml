<Window x:Class="AIHelper.Views.LiveChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="实时行情" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Background="#F0F0F0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="TxtInfo" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="刷新" Click="BtnRefresh_Click" Style="{StaticResource ButtonPrimary}"/>
        </Grid>
        
        <ProgressBar x:Name="LoadProgress" Grid.Row="1" Height="2" IsIndeterminate="True" BorderThickness="0" Foreground="#10b981"/>
        
        <wv2:WebView2 x:Name="WebView" Grid.Row="2"/>
    </Grid>
</Window>
