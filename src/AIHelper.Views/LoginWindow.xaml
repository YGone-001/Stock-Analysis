<Window x:Class="AIHelper.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="身份验证 - AIHelper" Height="400" Width="300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="{DynamicResource RegionBrush}" CornerRadius="8" MouseLeftButtonDown="DragWindow">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="身份验证" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <Button x:Name="BtnCancel" Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-10,-10,0" Click="Close_Click"/>
            
            <TextBox x:Name="TxtUsername" hc:InfoElement.Placeholder="代号 (Username)" Margin="0,0,0,15" Grid.Row="1"/>
            <PasswordBox x:Name="TxtPassword" hc:InfoElement.Placeholder="暗号 (Password)" Margin="0,0,0,15" Grid.Row="2"/>
            
            <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,0,0,15">
                <CheckBox x:Name="ChkSaveUser" Content="记住代号" Margin="0,0,10,0"/>
                <CheckBox x:Name="ChkSavePwd" Content="记住暗号" Margin="0,0,10,0"/>
                <CheckBox x:Name="ChkAutoLogin" Content="自动验证"/>
            </StackPanel>
            
            <TextBlock x:Name="TxtError" Grid.Row="4" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,15"/>
            
            <StackPanel Orientation="Vertical" Grid.Row="5" VerticalAlignment="Bottom">
                <Button x:Name="BtnLogin" Content="进 入 (Login)" Style="{DynamicResource ButtonPrimary}" Margin="0,0,0,10" Click="BtnLogin_Click"/>
                <Button x:Name="BtnRegister" Content="新身份 (Register)" Click="BtnRegister_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
