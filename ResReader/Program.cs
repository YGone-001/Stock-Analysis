using System; using System.Reflection; using System.Resources; class Program { static void Main() { var asm = Assembly.LoadFile(@"E:\AIHelper-dev\src\obj\Release\net8.0-windows\win-x64\AIHelper.dll"); var stream = asm.GetManifestResourceStream("AIHelper.g.resources"); if(stream == null) { Console.WriteLine("STREAM NULL"); return; } var reader = new ResourceReader(stream); foreach (System.Collections.DictionaryEntry entry in reader) Console.WriteLine(entry.Key); } }
